package com.example;

/**
 * Represents an external API that provides data.
 * In a real application, this would make HTTP calls or interact with a third-party service.
 */
public interface ExternalApi {
    String getData();
}

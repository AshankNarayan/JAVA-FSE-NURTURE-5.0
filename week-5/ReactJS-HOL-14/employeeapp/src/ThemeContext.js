import { createContext } from 'react';

// 5. Create a new file with the name as ThemeContext.js. 
// Define a new context in the file with the name as ThemeContext 
// and assign it a default value of 'light' and export it as default.
const ThemeContext = createContext('light');

export default ThemeContext;
